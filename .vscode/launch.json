{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Frontend local",
      "type": "pwa-chrome",
      "request": "launch",
      "preLaunchTask": "dev: frontend + worker",
      "url": "http://localhost:4200/",
      "webRoot": "${workspaceFolder}/frontend/src"
    }
  ]
}
